Amar Patel
Time for Assignment: 6 hours after update


Can directly input the Lorenz parameters via the Terminal.


The "ESC" key will the program

To use static void key functionality:

"up" key - rotate x axis (positively)
"down" key - rotate x axis (negatively)

"left" key - rotate y axis (psotively)
"right" key - rotate y axis (negatively)





To use static void special functionality:


The "v" character will reset the view of the program.
The "r" character will reset the program
The "q" character will end the program and the iterations reset
The "a" character will speed up the plotting
The "s" character will slow down the plotting
The "+" character will zoom in
The "-" character will zoom out


